/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Lecture: Hoisting
/*-----------------------------------------------------------

//console.log('you are' + calcAge(currenty, birthy));
//It work coz functions(no variables) are called in the moment we need them.
//So it can be in any place
function calcAge(currentyear,birhtyear){    
    return currentyear - birhtyear;
}
var currenty = prompt('insert current year : ');
var birthy = prompt('insert your birthyear: ');
console.log('you are ' + calcAge(currenty, birthy));


// or 


//console.log('retirement in: ' + retirement(birthyear)); 
//it doesn't work here coz variabes must be declared before using them.
var retirement = function(year){
    return (65 - (calcAge(currenty, birthy)));
}

console.log('retirement in: ' + retirement(birthyear));

*/

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//Accesibiliy of variables and functions are the same of c++ (no need to do that);
/*-----------------------------------------------------------*/

// I think this is far enough for this section of the course...





























